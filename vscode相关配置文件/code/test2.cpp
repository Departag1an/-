#include <bits/stdc++.h>
using namespace std; 
typedef long long ll;
const int N = 1e5 + 10;

int n, m;
int main () {
    cin >> n >> m;
    vector<int> a;
    vector<vector<int> > g(n + 1);
    while(m--) {
        int s, t;
        cin >> s >> t;
        cout << s << t << endl;
    }
    return 0;
}
